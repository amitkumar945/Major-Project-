[project]
name = "app"
version = "0.1.0"
dependencies = [ "flask>=3.0", "flask-cors>=4.0", "pymongo>=4.6", "pyjwt>=2.8", "python-dotenv>=1.0", "bcrypt>=4.1", "werkzeug>=3.0", "gunicorn>=21.2 ; sys_platform != 'win32'", "pytest>=8.0" ]
requires-python = "~=3.14.0"
